const { writeFileSync } = require("fs");

// for (let i = 1; i <= 1; i++) {
//   i + 1;
//   const { writeFileSync } = require("fs");
//   setInterval(() => {
//     writeFileSync("./content/big.txt", `hello world ${i}\n`, { flag: "a" });
//   }, 2000);
// }
